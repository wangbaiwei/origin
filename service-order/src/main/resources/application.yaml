server:
  port: 8007

spring:
  application:
    name: service-order
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/service-passenger-user?characterEncoding=utf8&connectTimeout=1000&socketTimeout=3000&autoReconnect=true&allowPublicKeyRetrieval=true&useUnicode=true&useSSL=false&serverTimezone=UTC
    username: youth
    password: youth
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848